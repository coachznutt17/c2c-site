import React from "react";

const AnnouncementBar: React.FC = () => {
  return (
    <div
      style={{
        backgroundColor: "#0d1b2a",
        color: "white",
        padding: "0.5rem 1rem",
        textAlign: "center",
        fontWeight: 500,
      }}
    >
      🚀 Welcome to Coach2Coach — Where Coaches Get Paid!
    </div>
  );
};

export default AnnouncementBar;
